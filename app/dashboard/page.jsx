import React from "react";
import { UserButton } from "@clerk/nextjs";
const Dashboard = () => {
  return (
    <UserButton>
      <div>Dashboard</div>
    </UserButton>
  );
};

export default Dashboard;
